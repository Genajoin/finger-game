# Покажи цифру! ✋

**Экспериментальная демонстрация распознавания жестов с помощью камеры и MediaPipe**

Это pet-проект, демонстрирующий работу с веб-камерой и распознавание количества поднятых пальцев на руке. Проект включает две реализации:

- **Python-версия** (`finger_game.py`) — классическая десктопная игра с OpenCV и Pygame
- **Web-версия** (`index.html`) — браузерное приложение на чистом JavaScript с MediaPipe

> ⚠️ **Важно!** Это экспериментальная демонстрация (proof-of-concept). Проект не является полноценным приложением — он не имеет финальной стилизации, полноценной обработки ошибок и не готов к продакшену.

## 🎮 Описание игры

Игра предназначена для обучения детей счёту на пальцах:

1. На экране появляется случайное число от 0 до 10
2. Ребёнок должен показать это количество пальцев на одной или двух руках
3. Система распознаёт количество пальцев и выдаёт обратную связь
4. При правильном ответе — звуковой сигнал и новое число через 4 секунды

## 📋 Требования

### Общие требования

- Python 3.8 или выше
- Современный браузер с поддержкой WebRTC (для Web-версии)

### Зависимости Python

- `opencv-python` — работа с камерой и изображениями
- `mediapipe` — распознавание жестов
- `pygame` — отрисовка графического интерфейса
- `numpy` — численные вычисления

## 🚀 Локальный запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/Genajoin/finger-game
cd finger-game
```

### 2. Создание виртуального окружения (рекомендуется)

```bash
python -m venv .venv
source .venv/bin/activate  # Linux/macOS
# или
.venv\Scripts\activate     # Windows
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Запуск Python-версии

```bash
python finger_game.py
```

### 5. Запуск Web-версии

Откройте файл `index.html` в браузере (Chrome, Firefox, Edge) или запустите локальный сервер:

```bash
# С использованием Python
python -m http.server 8000

# Затем откройте в браузере:
# http://localhost:8000/index.html
```

> ⚠️ **Важно:** Для работы камеры в браузере страница должна быть загружена через HTTPS или через `localhost`. Простое открытие файла через `file://` может не работать из-за ограничений безопасности браузера.

## 📁 Структура проекта

```
finger-game/
├── finger_game.py      # Python-версия игры
├── index.html          # Web-версия игры
├── requirements.txt    # Зависимости Python
├── .gitignore          # Исключения для Git
└── README.md           # Документация
```

## 🔧 Как это работает

### Распознавание пальцев

Алгоритм использует MediaPipe Hands для определения ключевых точек руки:

1. **4 пальца** (указательный, средний, безымянный, мизинец): проверяется, поднят ли кончик пальца выше основного сустава (Y-координата)
2. **Большой палец**: проверяется положение относительно IP-сустава с учётом ориентации руки (левая/правая)

### Архитектура

- **Python-версия**: OpenCV → MediaPipe → Pygame (отрисовка)
- **Web-версия**: MediaPipe Hands (CDN) → Canvas API (отрисовка)

## ⚠️ Ограничения и известные проблемы

- Распознавание может быть неточным при плохом освещении
- Не обрабатывается случай, когда руки перекрывают друг друга
- Web-версия требует разрешения на доступ к камере
- Нет сохранения прогресса или настроек

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл [`LICENSE`](LICENSE) для подробной информации.

## 👨‍💻 Автор

Создано как pet-проект для демонстрации возможностей MediaPipe и компьютерного зрения.

---

**Приятного обучения!** 🎓